type User {
    id: ID!
}

# The Root Query for the application
type Query {
    getUser(id: Int, offset: Int): User!
}

# The Root Mutation for the application
type Mutation {
    createUser(title: String!) : User!
}