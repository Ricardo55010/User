type UserDTO {
    id: ID!
    name: String
}

# The Root Query for the application
type Query {
    getUser(id: Int, offset: Int): UserDTO!
}

# The Root Mutation for the application
type Mutation {
    createUser(name: String!) : UserDTO!
    updateUser(name: String!, id: Int!) : UserDTO!
    deleteUser(id: Int!): UserDTO!
}